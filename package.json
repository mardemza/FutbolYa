{
  "name": "futbolya",
  "private": true,
  "version": "0.1.0",
  "description": "FutbolYa monorepo (NestJS + React + TypeScript)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run start:dev -w api\" \"npm run dev -w web\"",
    "dev:api": "npm run start:dev -w api",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w api && npm run build -w web",
    "test": "npm run test -w api",
    "lint": "npm run lint -w api && npm run lint -w web"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
